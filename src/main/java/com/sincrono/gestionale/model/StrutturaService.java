package com.sincrono.gestionale.model;

import org.springframework.data.jpa.repository.JpaRepository;

public interface StrutturaService extends JpaRepository<Struttura, Integer> {
	/*public boolean aggiungi(Struttura s);
	public boolean elimina(int id);
	public boolean modifica(int id);*/
}
